apiVersion: v1
kind: Secret
metadata:
  name: {{ include "todo-app.fullname" . }}-auth-secret
  labels:
    {{- include "todo-app.labels" . | nindent 4 }}
type: Opaque
data:
  secret: {{ .Values.backend.env.authSecret | b64enc | quote }}